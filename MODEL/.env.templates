# === Domain Configuration ===
DOMAIN=meidai.ru

# Email used for Let's Encrypt registration
ACME_EMAIL=dev@md-archive.com

# === Traefik Authentication ===
# Use: `openssl passwd -apr1 yourpassword` to generate the hash
TRAEFIK_BASIC_AUTH=user:$$apr1$$yourhashedpassword

# === PostgreSQL ===
POSTGRES_PASSWORD=your_postgres_password

# === Redis ===
REDIS_PASSWORD=your_redis_password

# === Odoo ===
ODOO_ADMIN=your_odoo_master_admin_password

# === Directory Structure (for Volume Mounting) ===
INTEGRATOR_NAME=your_integrator_name
COMPANY_NAME=your_company_name
